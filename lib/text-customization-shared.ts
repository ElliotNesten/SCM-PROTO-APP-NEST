export function normalizeTextCustomizationKey(value: string) {
  return value.replace(/\s+/g, " ").trim();
}

export function normalizeTextCustomizationValue(value: string) {
  return value.replace(/\s+/g, " ").trim();
}
